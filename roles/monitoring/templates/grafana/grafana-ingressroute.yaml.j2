kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints: 
    - web
  routes:
  - match: Host(`grafana{{ suffix }}.local.lan`) 
    kind: Rule
    services:
    - name: grafana
      port: 3000