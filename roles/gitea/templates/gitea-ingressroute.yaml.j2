kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: gitea
  namespace: gitea
spec:
  entryPoints: 
    - web
  routes:
  - match: Host(`gitea{{ suffix }}.local.lan`) 
    kind: Rule
    services:
    - name: gitea-http
      port: 3000