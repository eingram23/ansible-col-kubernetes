apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: unifi-tls
  namespace: unifi
spec:
  secretName: unifi-tls
  issuerRef:
    kind: ClusterIssuer
    name: vault-cluster-issuer
  commonName: unifi{{ suffix }}.local.lan
  dnsNames:
  - unifi{{ suffix }}.local.lan
  duration: 8760h