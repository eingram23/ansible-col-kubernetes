apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: local-lan
  namespace: vault
spec:
  secretName: local-lan-tls
  issuerRef:
    name: vault-issuer
  commonName: www.local.lan
  dnsNames:
  - www.local.lan
