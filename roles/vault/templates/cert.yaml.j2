apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ app }}
  namespace: default
spec:
  secretName: {{ app }}-tls
  issuerRef:
    name: vault-issuer
  commonName: www.example.com
  dnsNames:
  - www.example.com