apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-test2-pvc
  namespace: test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
