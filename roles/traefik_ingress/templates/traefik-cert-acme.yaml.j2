apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-tls-acme
  namespace: kube-system
spec:
  secretName: traefik-tls
  issuerRef:
    kind: ClusterIssuer
    name: acme-cluster-issuer
  commonName: traefik{{ suffix }}.ycdisp.net
  dnsNames:
  - traefik{{ suffix }}.ycdisp.net