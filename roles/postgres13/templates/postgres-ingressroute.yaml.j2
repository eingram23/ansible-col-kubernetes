kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: postgres
  namespace: postgres
spec:
  entryPoints: 
    - web
  routes:
  - match: Host(`postgres{{ suffix }}.local.lan`) 
    kind: Rule
    services:
    - name: postgres
      port: 5432
