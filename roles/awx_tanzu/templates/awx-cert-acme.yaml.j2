apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: awx-tls-acme
  namespace: awx
spec:
  secretName: awx-tls
  issuerRef:
    kind: ClusterIssuer
    name: acme-cluster-issuer
  commonName: awx{{ suffix }}.ycdisp.net
  dnsNames:
  - awx{{ suffix }}.ycdisp.net
