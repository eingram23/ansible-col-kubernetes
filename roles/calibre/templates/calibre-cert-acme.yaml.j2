apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: calibre-tls-acme
  namespace: calibre
spec:
  secretName: calibre-tls
  issuerRef:
    kind: ClusterIssuer
    name: acme-cluster-issuer
  commonName: calibre{{ suffix }}.ycdisp.net
  dnsNames:
  - calibre{{ suffix }}.ycdisp.net
