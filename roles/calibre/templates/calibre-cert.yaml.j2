apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: calibre{{ suffix }}.local.lan
  namespace: calibre
spec:
  secretName: calibre-dev.local.lan
  issuerRef:
    kind: ClusterIssuer
    name: vault-cluster-issuer
  commonName: calibre{{ suffix }}.local.lan
  dnsNames:
  - calibre{{ suffix }}.local.lan