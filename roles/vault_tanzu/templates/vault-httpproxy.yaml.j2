---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: vault-ui
  namespace: vault
spec:
  virtualhost:
    fqdn: vault{{ suffix }}.local.lan
    tls:
      passthrough: true
  tcpproxy:
    services:
    - name: vault-ui
      port: 8200
